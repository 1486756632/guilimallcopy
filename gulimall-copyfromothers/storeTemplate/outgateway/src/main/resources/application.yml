# Tomcat
server:
  tomcat:
    uri-encoding: UTF-8
    max-threads: 1000
    min-spare-threads: 30
  port: 1400
  connection-timeout: 5000ms
  servlet:
    context-path: /outGateway

spring:
  application:
    name: storeTemplate.outGateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848